# Procédure de Test — v2.2 Workflow Objets

**Version** : 2.2.0
**Date** : 2026-05-23
**Branche** : `feature/v2.2-workflow-objets`
**Issues** : #136 #139 #140 #141 #142 #143 #144
**Testeur** : QA

---

## Prérequis

- [ ] **Environnement** : QUALIF (Windows 10/11 x64, backend WebDAV actif)
- [ ] **Build** : `wails build` réussi sur la branche feature (exe Windows)
- [ ] **Backend WebDAV** : serveur local ou distant accessible et connecté dans GhostDrive
- [ ] **Dossier GhostDrive** : un backend configuré et synchronisé (au moins 2 fichiers en état ✓✓)
- [ ] **Droits** : accès Explorateur Windows + droits d'écriture sur le dossier local

---

## Scénario 1 — Rename natif via Explorateur Windows (#139)

**Objectif** : Vérifier que renommer un fichier synchronisé dans l'Explorateur déclenche un
MOVE WebDAV (pas un delete+upload) et que le fichier apparaît sous son nouveau nom sur le serveur.

**Prérequis spécifiques** :
- Fichier `test-rename.txt` uploadé et en état ✓✓ (Synced) dans le dossier GhostDrive

| Étape | Action | Résultat Attendu | Résultat Obtenu | OK ? |
|-------|--------|-----------------|----------------|------|
| 1 | Ouvrir l'Explorateur sur le dossier GhostDrive | Fichier `test-rename.txt` visible avec badge ✓✓ | | |
| 2 | Renommer `test-rename.txt` en `test-renamed.txt` (F2 ou clic lent) | Fichier renommé immédiatement en local | | |
| 3 | Attendre 2–3 secondes | Badge ✓✓ (ou ⟳ puis ✓✓) sur `test-renamed.txt` | | |
| 4 | Vérifier sur le serveur WebDAV (client WebDAV ou navigateur) | `test-renamed.txt` présent, `test-rename.txt` absent | | |
| 5 | Vérifier les logs GhostDrive (icône systray → Logs) | Ligne `ActionRename` (ou `MOVE`) sans `ActionDelete` ni `ActionUpload` pour ce fichier | | |

**Verdict** : [ ] PASS  [ ] FAIL

---

## Scénario 2 — Rename fallback (backend sans MOVE) (#139)

**Objectif** : Vérifier le fallback Delete+Upload quand le backend ne supporte pas le rename natif.

**Prérequis spécifiques** :
- Backend configuré qui retourne `ErrNotSupported` sur Rename (ou backend `local` pour simuler)
- Fichier `fallback-rename.txt` uploadé et Synced

| Étape | Action | Résultat Attendu | Résultat Obtenu | OK ? |
|-------|--------|-----------------|----------------|------|
| 1 | Renommer `fallback-rename.txt` en `fallback-new.txt` | Fichier renommé en local | | |
| 2 | Attendre 5–10 secondes | Badge ✓✓ sur `fallback-new.txt` | | |
| 3 | Vérifier côté serveur | `fallback-new.txt` présent (uploadé), `fallback-rename.txt` absent (supprimé) | | |
| 4 | Vérifier logs | Ligne `rename fallback upload` suivie de `rename fallback delete` | | |
| 5 | Vérifier qu'aucune erreur n'est affichée dans l'UI | Pas de badge ❌ ni notification d'erreur | | |

**Verdict** : [ ] PASS  [ ] FAIL

---

## Scénario 3 — Supprimer un fichier Local Only (#141)

**Objectif** : Vérifier qu'un fichier créé localement mais jamais synchronisé n'est pas
supprimé sur le remote quand l'utilisateur le supprime localement.

**Prérequis spécifiques** :
- GhostDrive configuré avec un backend WebDAV
- Pauser la synchronisation avant de créer le fichier (icône systray → Pause)

| Étape | Action | Résultat Attendu | Résultat Obtenu | OK ? |
|-------|--------|-----------------|----------------|------|
| 1 | Créer `local-only.txt` dans le dossier GhostDrive (pendant que sync est pausée) | Fichier créé localement, aucun badge (ou badge ☁️ absent sur le remote) | | |
| 2 | Vérifier côté serveur | `local-only.txt` N'EXISTE PAS sur le remote | | |
| 3 | Supprimer `local-only.txt` dans l'Explorateur | Fichier supprimé localement | | |
| 4 | Reprendre la synchronisation (icône systray → Reprendre) | Aucune action sur le remote | | |
| 5 | Vérifier côté serveur après 3 secondes | `local-only.txt` toujours absent du remote (pas de delete spurieux) | | |
| 6 | Vérifier logs | Aucune ligne `ActionDelete` pour `local-only.txt` | | |

**Verdict** : [ ] PASS  [ ] FAIL

---

## Scénario 4 — Unpin fichier non synchronisé (#142)

**Objectif** : Vérifier que désépingler (libérer en tant que placeholder) un fichier qui n'a
pas encore été uploadé déclenche l'upload d'abord, puis la libération CF.

**Prérequis spécifiques** :
- Dossier GhostDrive actif avec un backend WebDAV
- GhostDrive version ≥ 2.2 (séquence atomique unpin)

| Étape | Action | Résultat Attendu | Résultat Obtenu | OK ? |
|-------|--------|-----------------|----------------|------|
| 1 | Pauser la sync | Badge ⏸ sur l'icône systray | | |
| 2 | Créer `unpin-test.txt` dans le dossier GhostDrive | Fichier local uniquement | | |
| 3 | Clic droit sur `unpin-test.txt` → "Libérer de l'espace" (CloudOnly) | Action en attente (sync pausée) | | |
| 4 | Reprendre la sync | Déclenchement de l'upload | | |
| 5 | Attendre la complétion de l'upload (badge ✓✓ ou ☁️) | Badge ☁️ (placeholder) après upload réussi | | |
| 6 | Vérifier côté serveur | `unpin-test.txt` présent sur le remote | | |
| 7 | Vérifier que le fichier est maintenant un placeholder | Taille locale = 0 ou accès déclenche une hydratation | | |

**Verdict** : [ ] PASS  [ ] FAIL

---

## Scénario 5 — Rollback si upload échoue lors de l'unpin (#142)

**Objectif** : Vérifier que si l'upload échoue pendant l'unpin, le fichier reste accessible
localement (pas de perte de données).

**Prérequis spécifiques** :
- Backend WebDAV simulant une erreur d'upload (couper le réseau ou configurer un backend invalide temporairement)

| Étape | Action | Résultat Attendu | Résultat Obtenu | OK ? |
|-------|--------|-----------------|----------------|------|
| 1 | Créer `rollback-test.txt` dans le dossier GhostDrive | Fichier local uniquement | | |
| 2 | Couper la connexion au backend | Backend marqué offline dans l'UI | | |
| 3 | Tenter de désépingler `rollback-test.txt` | Erreur signalée à l'utilisateur | | |
| 4 | Vérifier que `rollback-test.txt` est toujours accessible localement | Fichier complet, non corrompu | | |
| 5 | Restaurer la connexion et réessayer le désépinglage | Upload puis libération CF réussis | | |

**Verdict** : [ ] PASS  [ ] FAIL

---

## Scénario 6 — Pin pendant un upload actif (#143)

**Objectif** : Vérifier que cliquer "Épingler" sur un fichier en cours d'upload enfile
l'intention et l'exécute après la complétion du transfert.

**Prérequis spécifiques** :
- Fichier volumineux (>50 Mo) pour avoir le temps d'interagir pendant l'upload
- Backend avec débit limité pour allonger le transfert

| Étape | Action | Résultat Attendu | Résultat Obtenu | OK ? |
|-------|--------|-----------------|----------------|------|
| 1 | Placer un fichier volumineux dans le dossier GhostDrive | Upload démarre (badge ⟳ + barre de progression) | | |
| 2 | Pendant l'upload (progression 10–80 %), clic droit → "Toujours garder sur cet appareil" | Pas d'erreur immédiate ; action en attente | | |
| 3 | Laisser l'upload se terminer | Badge ✓✓ puis badge ⚡ (pinned) | | |
| 4 | Vérifier côté serveur | Fichier uploadé intégralement | | |
| 5 | Vérifier état final local | Fichier épinglé (non-placeholder), contenu complet | | |

**Verdict** : [ ] PASS  [ ] FAIL

---

## Scénario 7 — Toast de conflit (#144)

**Objectif** : Vérifier qu'une modification simultanée d'un fichier (local + remote) déclenche
un toast non-bloquant dans l'UI GhostDrive.

**Prérequis spécifiques** :
- Backend WebDAV accessible depuis deux clients (ou modification directe via client WebDAV)

| Étape | Action | Résultat Attendu | Résultat Obtenu | OK ? |
|-------|--------|-----------------|----------------|------|
| 1 | Pauser GhostDrive | Sync arrêtée | | |
| 2 | Modifier `conflict-test.txt` localement (ajouter du texte et sauvegarder) | Modification locale enregistrée | | |
| 3 | Modifier `conflict-test.txt` côté serveur (via client WebDAV, heure plus récente) | Modification distante enregistrée | | |
| 4 | Reprendre GhostDrive | Sync redémarre | | |
| 5 | Attendre la réconciliation | Toast ambre affiché en bas à droite : "Conflit détecté : conflict-test.txt (résolu automatiquement)" | | |
| 6 | Lire le toast | Icône ⚠️ amber, message lisible, bouton × visible | | |
| 7 | Cliquer sur le toast ou le bouton × | Toast dismissed immédiatement | | |
| 8 | Ne pas interagir (laisser 5 secondes) | Toast auto-dismissed après 5 s | | |

**Verdict** : [ ] PASS  [ ] FAIL

---

## Scénario 8 — Flood de conflits → collapse (#144)

**Objectif** : Vérifier que plus de 3 conflits simultanés provoquent un collapse en un seul toast résumé.

| Étape | Action | Résultat Attendu | Résultat Obtenu | OK ? |
|-------|--------|-----------------|----------------|------|
| 1 | Créer 5 conflits simultanément (modifier 5 fichiers localement ET remotement pendant pause) | — | | |
| 2 | Reprendre la sync | Les 3 premiers toasts s'affichent individuellement, puis collapse automatique | | |
| 3 | Observer le toast résumé | "N conflits détectés (résolus automatiquement)" — valeur N correcte | | |
| 4 | Dismisser le toast résumé | Pile vide, compteur réinitialisé | | |

**Verdict** : [ ] PASS  [ ] FAIL

---

## Critères de Validation Globaux

- [ ] Scénario 1 PASS : rename natif (MOVE WebDAV) fonctionnel sans delete+upload
- [ ] Scénario 2 PASS : fallback rename (delete+upload) fonctionnel sans erreur
- [ ] Scénario 3 PASS : suppression Local Only ne déclenche aucune action remote (#141 résolu)
- [ ] Scénario 4 PASS : unpin déclenche upload avant libération CF (#142 résolu)
- [ ] Scénario 5 PASS : rollback unpin si upload échoue — pas de perte de données (#142)
- [ ] Scénario 6 PASS : pin pendant upload enfilé et exécuté post-transfert (#143 résolu)
- [ ] Scénario 7 PASS : toast conflit affiché, non-bloquant, auto-dismiss 5 s (#144 résolu)
- [ ] Scénario 8 PASS : collapse > 3 conflits → toast résumé (#144)
- [ ] Aucune régression détectée sur la synchronisation basique (upload/download/delete)
- [ ] Aucun crash ou data-loss observé sur les scénarios d'erreur

---

## Notes QA

*Espace pour observations, captures d'écran, logs à joindre au rapport.*

---
*Procédure générée automatiquement par l'agent test-writer — v2.2 Workflow Objets*
